package Utilities;

public interface timezone {

    /**
     * Shows the user's time zone via lambda. Can use the lambda to
     * display the user's time zone on any menu (currently implemented only on the login menu).
     */
    void printTimeZone();
}